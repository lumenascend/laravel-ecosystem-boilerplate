{
	"name": "Existing Docker Compose (Extend)",
	"dockerComposeFile": [
		"../docker-compose.yml"
	],
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker": "latest"
	},
	"service": "laravel.test",
	"workspaceFolder": "/var/www/html",
	"customizations": {
		"vscode": {
			"extensions": [
				"mtxr.sqltools",
				"mtxr.sqltools-driver-mysql",
				"GitHub.copilot",
				"DEVSENSE.phptools-vscode"
			],
			"settings": {}
		}
	},
	"remoteUser": "sail",
	"postCreateCommand": "php -r \"file_exists('.env') || copy('.env.example', '.env');\" && chown -R 1000:1000 /var/www/html 2>/dev/null || true && composer config http-basic.nova.laravel.com $NOVA_USERNAME $NOVA_LICENSE_KEY && composer install && npm install"
	// "forwardPorts": [],
	// "runServices": [],
	// "shutdownAction": "none",
}
